name: pr voter

on: [pull_request]

jobs:
  build-deploy:
    runs-on: ubuntu-20.04

    steps:
    - uses: actions/checkout@v2

    - uses: dlang-community/setup-dlang@v1
      with:
        compiler: dmd-latest
    - name: Run validation
      run: dub -- validate
